```
┌──────────────────────────────────────────────────────────────────────────────┐
│                      YOLO PIPELINE CODE STRUCTURE                            │
└──────────────────────────────────────────────────────────────────────────────┘

📱 SCANNER SCREEN (lib/screens/scanner_screen.dart)
┌──────────────────────────────────────────────────────────────────────────────┐
│                                                                              │
│  ┌────────────────────────────────────────────────────────────────────┐     │
│  │  initState()                                                       │     │
│  │    ├─ _initializeCamera()  ──────────────┐                        │     │
│  │    └─ _initializeDetector() ──────┐      │                        │     │
│  └────────────────────────────────────┼──────┼────────────────────────┘     │
│                                       │      │                               │
│  ┌────────────────────────────────────▼──────▼────────────────────────┐     │
│  │  Camera Controller                                                 │     │
│  │    └─ startImageStream((CameraImage frame) {                      │     │
│  │          _detectionProvider.processFrame(frame); ◄─── STEP 1      │     │
│  │       })                                                           │     │
│  └────────────────────────────────────────────────────────────────────┘     │
│                                       │                                      │
│                                       │ Camera Frame                         │
│                                       ▼                                      │
└──────────────────────────────────────────────────────────────────────────────┘
                                        │
                                        │
┌───────────────────────────────────────▼──────────────────────────────────────┐
│  DETECTION PROVIDER (lib/providers/detection_provider.dart)                  │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  processFrame(CameraImage cameraImage) {                                    │
│    if (!_isRunning || !_isInitialized) return;                             │
│                                                                              │
│    final detections = await detector.detect(cameraImage); ◄─── Calls       │
│    _detections = detections;                                                │
│    notifyListeners(); ──────────────────┐                                   │
│  }                                      │                                   │
│                                         │                                   │
└─────────────────────────────────────────┼───────────────────────────────────┘
                                          │
                          Updates UI ─────┘
                                          │
                                          ▼
┌──────────────────────────────────────────────────────────────────────────────┐
│  UI UPDATE (Scanner Screen - Consumer<DetectionProvider>)                    │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  Consumer<DetectionProvider>(                                               │
│    builder: (context, detectionProvider, _) {                               │
│      return CustomPaint(                                                    │
│        painter: DetectionPainter(                                           │
│          detections: detectionProvider.detections, ◄─── STEP 6              │
│          imageSize: cameraSize,                                             │
│          canvasSize: screenSize,                                            │
│        ),                                                                   │
│      );                                                                     │
│    },                                                                       │
│  )                                                                          │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘




┌──────────────────────────────────────────────────────────────────────────────┐
│  YOLO DETECTOR (lib/services/yolo_detector.dart)                            │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  detect(CameraImage cameraImage) async {                                    │
│                                                                              │
│    ┌─────────────────────────────────────────────────────────────────┐      │
│    │  STEP 2: PREPROCESSING                                          │      │
│    │  ────────────────────────────────────────────────────────       │      │
│    │  final input = _prepareInput(cameraImage);                     │      │
│    │                                                                 │      │
│    │  _prepareInput() {                                             │      │
│    │    1. Create tensor [1, 640, 640, 3]                          │      │
│    │    2. Resize image: original → 640x640                        │      │
│    │    3. Convert color: YUV/BGRA → RGB                          │      │
│    │    4. Normalize: 0-255 → 0.0-1.0                             │      │
│    │    5. Fill tensor with normalized RGB values                  │      │
│    │    return input; // [1, 640, 640, 3]                         │      │
│    │  }                                                             │      │
│    └─────────────────────────────────────────────────────────────────┘      │
│                              │                                              │
│                              ▼                                              │
│    ┌─────────────────────────────────────────────────────────────────┐      │
│    │  STEP 3: YOLO MODEL INFERENCE                                  │      │
│    │  ────────────────────────────────────────────────────────       │      │
│    │  final output = [[[0.0]...]]; // [1, 29, 8400]                │      │
│    │  _interpreter.run(input, output);                             │      │
│    │                                                                 │      │
│    │  Model File: assets/models/best_float32.tflite               │      │
│    │  Input:  [1, 640, 640, 3] - Normalized RGB image             │      │
│    │  Output: [1, 29, 8400]    - Raw predictions                  │      │
│    │                                                                 │      │
│    │  29 channels per prediction:                                   │      │
│    │    • Channels 0-3: Bounding box (x, y, w, h)                  │      │
│    │    • Channel 4:    Confidence score                            │      │
│    │    • Channels 5-28: Class scores (24 classes)                 │      │
│    │                                                                 │      │
│    │  8400 predictions from 3 detection scales:                     │      │
│    │    • 80x80 grid   = 6400 predictions                           │      │
│    │    • 40x40 grid   = 1600 predictions                           │      │
│    │    • 20x20 grid   = 400 predictions                            │      │
│    └─────────────────────────────────────────────────────────────────┘      │
│                              │                                              │
│                              ▼                                              │
│    ┌─────────────────────────────────────────────────────────────────┐      │
│    │  STEP 4: PARSE RAW OUTPUTS                                     │      │
│    │  ────────────────────────────────────────────────────────       │      │
│    │  final detections = _parseOutput(output[0]);                  │      │
│    │                                                                 │      │
│    │  _parseOutput(output) {                                        │      │
│    │    for (i = 0; i < 8400; i++) {                               │      │
│    │      // Extract from each prediction:                          │      │
│    │      x = output[0][i];          // X coordinate               │      │
│    │      y = output[1][i];          // Y coordinate               │      │
│    │      w = output[2][i];          // Width                      │      │
│    │      h = output[3][i];          // Height                     │      │
│    │      confidence = output[4][i]; // Confidence score           │      │
│    │                                                                 │      │
│    │      // Find highest class score                               │      │
│    │      classId = argmax(output[5..28][i]);                      │      │
│    │      label = currencyClasses[classId];                        │      │
│    │                                                                 │      │
│    │      // Filter by confidence                                   │      │
│    │      if (confidence >= 0.1) {                                 │      │
│    │        detections.add(CurrencyDetection(...));                │      │
│    │      }                                                          │      │
│    │    }                                                            │      │
│    │    return detections;                                          │      │
│    │  }                                                              │      │
│    └─────────────────────────────────────────────────────────────────┘      │
│                              │                                              │
│                              ▼                                              │
│    ┌─────────────────────────────────────────────────────────────────┐      │
│    │  STEP 5: POST-PROCESSING (NMS)                                 │      │
│    │  ────────────────────────────────────────────────────────       │      │
│    │  final filtered = _applyNMS(detections);                      │      │
│    │                                                                 │      │
│    │  _applyNMS(detections) {                                       │      │
│    │    // Sort by confidence (highest first)                       │      │
│    │    detections.sort((a, b) => b.conf - a.conf);                │      │
│    │                                                                 │      │
│    │    result = [];                                                │      │
│    │    suppressed = [false, false, ...];                           │      │
│    │                                                                 │      │
│    │    for (i in detections) {                                     │      │
│    │      if (suppressed[i]) continue;                              │      │
│    │      result.add(detections[i]);                                │      │
│    │                                                                 │      │
│    │      // Suppress overlapping boxes                             │      │
│    │      for (j in remaining) {                                    │      │
│    │        iou = calculateIoU(box[i], box[j]);                    │      │
│    │        if (iou > 0.5) {                                       │      │
│    │          suppressed[j] = true; // Remove duplicate            │      │
│    │        }                                                        │      │
│    │      }                                                          │      │
│    │    }                                                            │      │
│    │    return result; // Final unique detections                  │      │
│    │  }                                                              │      │
│    │                                                                 │      │
│    │  IoU Calculation:                                              │      │
│    │    intersection = overlap_area(box1, box2)                    │      │
│    │    union = area(box1) + area(box2) - intersection            │      │
│    │    iou = intersection / union                                  │      │
│    └─────────────────────────────────────────────────────────────────┘      │
│                              │                                              │
│                              ▼                                              │
│    return filtered; // Final detections ready for display                  │
│  }                                                                          │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘




┌──────────────────────────────────────────────────────────────────────────────┐
│  DETECTION PAINTER (lib/painters/detection_painter.dart)                    │
├──────────────────────────────────────────────────────────────────────────────┤
│                                                                              │
│  paint(Canvas canvas, Size size) {                                          │
│                                                                              │
│    for (detection in detections) {                                          │
│                                                                              │
│      ┌────────────────────────────────────────────────────────────┐         │
│      │  COORDINATE TRANSFORMATION                                 │         │
│      │  ─────────────────────────────────────────────────────     │         │
│      │  // Scale from normalized (0-1) to screen pixels          │         │
│      │  scaleX = canvasWidth / imageWidth;                       │         │
│      │  scaleY = canvasHeight / imageHeight;                     │         │
│      │                                                             │         │
│      │  // Convert center + size to top-left corner              │         │
│      │  left = (x - width/2) * scaleX;                           │         │
│      │  top = (y - height/2) * scaleY;                           │         │
│      │  right = (x + width/2) * scaleX;                          │         │
│      │  bottom = (y + height/2) * scaleY;                        │         │
│      └────────────────────────────────────────────────────────────┘         │
│                                                                              │
│      ┌────────────────────────────────────────────────────────────┐         │
│      │  DRAW BOUNDING BOX                                         │         │
│      │  ─────────────────────────────────────────────────────     │         │
│      │  // Color based on confidence                              │         │
│      │  hue = confidence * 120; // Green for high conf            │         │
│      │  color = HSVColor(1, hue, 1, 1).toColor();                │         │
│      │                                                             │         │
│      │  // Draw rectangle                                         │         │
│      │  canvas.drawRect(                                          │         │
│      │    Rect.fromLTRB(left, top, right, bottom),               │         │
│      │    Paint(color, strokeWidth: 3)                           │         │
│      │  );                                                         │         │
│      └────────────────────────────────────────────────────────────┘         │
│                                                                              │
│      ┌────────────────────────────────────────────────────────────┐         │
│      │  DRAW LABEL                                                │         │
│      │  ─────────────────────────────────────────────────────     │         │
│      │  text = "${label} ${confidence*100}%";                    │         │
│      │  // e.g., "500_front 92.3%"                               │         │
│      │                                                             │         │
│      │  // Background box                                         │         │
│      │  canvas.drawRect(labelBackground, blackColor);            │         │
│      │                                                             │         │
│      │  // White text                                             │         │
│      │  canvas.drawText(text, position, whiteColor);             │         │
│      └────────────────────────────────────────────────────────────┘         │
│    }                                                                         │
│  }                                                                           │
│                                                                              │
└──────────────────────────────────────────────────────────────────────────────┘


═══════════════════════════════════════════════════════════════════════════════
                              DATA STRUCTURES
═══════════════════════════════════════════════════════════════════════════════

class CurrencyDetection {
  double confidence;  // 0.0 to 1.0
  String label;       // e.g., "500_front", "Gandhi_potrait"
  double x;           // Center X (normalized 0-1)
  double y;           // Center Y (normalized 0-1)
  double width;       // Box width (normalized 0-1)
  double height;      // Box height (normalized 0-1)
}

24 Currency Classes:
  - Denominations: 100_front, 100_back, 200_front, 200_back, 500_front, 
                   500_back, 50_front, 50_back
  - Security: security_thread, bleed_lines, color_chg_num, micro_text
  - Features: Gandhi_potrait, ashoka_piller, res_bank, serial_number, etc.

═══════════════════════════════════════════════════════════════════════════════
                           PERFORMANCE OPTIMIZATIONS
═══════════════════════════════════════════════════════════════════════════════

1. Frame Skipping:
   _frameSkipCounter % 2 != 0 → Skip every other frame

2. Concurrent Prevention:
   if (_isProcessingFrame) return; → One frame at a time

3. Early Exit:
   if (confidence < 0.1) continue; → Skip low confidence

4. Efficient NMS:
   Sort once + suppress overlaps → O(n²) but necessary

═══════════════════════════════════════════════════════════════════════════════
```
